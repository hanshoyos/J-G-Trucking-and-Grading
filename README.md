# J&G Trucking and Grading

This project includes a basic test suite using `pytest`.

## Running tests

Ensure you have Python installed. From the project root, install `pytest` (if not already installed) and run:

```bash
pip install pytest
pytest
```
