{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ace-correlate.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ace-correlate.labels" . | nindent 4 }}
{{- end }}
